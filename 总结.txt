信息抽取（IE）：命名实体识别（NER）、关系抽取（RE）、事件抽取（EE）

关系抽取：若有两个存在着关系的实体，我们可将两个实体分别成为主体和客体，那么关系抽取就是在非结构或半结构化数据中找出主体与客体之间存在的关系，并将其表示为实体关系三元组，即（主体，关系，客体）。

事件抽取：从半结构化、非结构化数据中，将与我们的目标相关的事件的重要元素识别出来。
比如：描述“企业成立”这样一种事件，需要成立时间、创立者、企业名称、企业业务范围等等信息，
事件抽取相关概念：事件描述、事件类型、事件触发词、事件要素（地点、 人物等）、要素的角色。
